# starship 併用 status codeが129以上か複数ある場合のみ表示.

local status_array=(${pipestatus[*]})

if [[ ${#status_array[*]} > 1 || ${status_array[1]} > 128 ]]; then
    autoload -Uz _prompt-pipestatus
    _prompt-pipestatus $status_array
else
    _prompt_pipestatus_var=''
fi
